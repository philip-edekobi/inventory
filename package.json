{
  "name": "inventory",
  "version": "1.0.0",
  "description": "Miniature Inventory Application",
  "main": "index.js",
  "scripts": {
    "start": "cd server && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "author": "Mike & Phil",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.13",
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": "16.13.0"
  }
}
